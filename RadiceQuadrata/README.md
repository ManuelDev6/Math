# Calcola Radice Quadrata
Un programma che chiede un numero e ne calcola la radice quadrata tramite la funzione sqrt.

## Funzione sqrt

La funzione sqrt accetta un solo parametro(il numero) e ne calcola la radice quadrata
