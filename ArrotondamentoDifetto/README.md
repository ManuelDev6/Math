# Arrotondamento per difetto
Un programma che chiede un numero in virgola mobile e lo arrotonda per difetto.

## Funzione floor

La funzione floor() accetta un solo parametro(il numero) e lo arrotonda per difetto.
