# Arrotondamento per eccesso
Un programma che chiede un numero in virgola mobile e lo arrotonda per eccesso.

## Funzione ceil

La funzione ceil() accetta un solo parametro(il numero) e lo arrotonda per eccesso.
